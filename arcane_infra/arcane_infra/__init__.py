"""Infrastructure and training orchestration for Arcane.

Provides the bridge between ``arcane train`` CLI commands and the
actual training frameworks (TRL, veRL) + cloud provisioning (SkyPilot).
"""
