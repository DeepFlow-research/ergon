/**
 * Re-export useSocket hook from SocketProvider.
 * 
 * This file exists for consistent import paths:
 * import { useSocket } from "@/hooks/useSocket";
 */
export { useSocket } from "@/providers/SocketProvider";
