/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using custom server with Socket.io (not standalone mode)
  // The custom server in server.ts integrates Next.js with Socket.io
};

export default nextConfig;
