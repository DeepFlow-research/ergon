# Multi-node TRL GRPO training with Ergon environments.
# TODO: git clone below requires the repo to be public. Until then, use
#       file_mounts to sync local source (see /tmp/ac2-gpu-test.yaml pattern).
#
# Node 0 runs `trl vllm-serve` (inference).
# Node 1+ runs `ergon train local` (training with DeepSpeed ZeRO-3).
# Weight sync between training and vLLM happens via NCCL automatically.
#
#   sky launch ergon_infra/ergon_infra/templates/train-trl-multinode.yaml -y \
#     --env DEFINITION_ID=<uuid> \
#     --env MODEL=Qwen/Qwen2.5-1.5B

name: ergon-training-multinode

resources:
  accelerators: ${GPU_TYPE:-A10G}:${GPU_COUNT:-1}
  infra: shadeform
num_nodes: ${NUM_NODES:-2}

file_mounts:
  /checkpoints:
    source: ${CHECKPOINT_BUCKET:-s3://my-ergon-checkpoints}
    mode: MOUNT_CACHED

envs:
  DEFINITION_ID:
  MODEL:
  BENCHMARK: ${BENCHMARK:-smoke-test}
  VLLM_TP_SIZE: ${VLLM_TP_SIZE:-1}
  SAVE_STEPS: ${SAVE_STEPS:-50}
  NUM_GENERATIONS: ${NUM_GENERATIONS:-4}
  LEARNING_RATE: ${LEARNING_RATE:-1e-5}
  TIMEOUT: ${TIMEOUT:-300}

setup: |
  git clone https://github.com/DeepFlow-research/ergon.git /tmp/ergon
  cd /tmp/ergon
  pip install -e ./ergon_core -e ./ergon_builtins -e ./ergon_cli -e ./ergon_infra
  pip install "trl[vllm]>=1.0.0,<2.0.0" datasets transformers

run: |
  HEAD_IP=$(echo "$SKYPILOT_NODE_IPS" | head -n1)

  if [ "$SKYPILOT_NODE_RANK" = "0" ]; then
    echo "Node 0: starting vLLM serve (model=$MODEL, tp=$VLLM_TP_SIZE)"
    trl vllm-serve \
      --model "$MODEL" \
      --tensor_parallel_size "$VLLM_TP_SIZE" \
      --host 0.0.0.0 \
      --port 8000
  else
    echo "Node $SKYPILOT_NODE_RANK: waiting for vLLM on $HEAD_IP:8000..."
    for i in $(seq 1 180); do
      if curl -s "http://$HEAD_IP:8000/health" > /dev/null 2>&1; then
        echo "vLLM ready after ${i}s"
        break
      fi
      sleep 1
    done

    ergon train local \
      --benchmark "$BENCHMARK" \
      --definition-id "$DEFINITION_ID" \
      --model "$MODEL" \
      --vllm-mode server \
      --vllm-server-url "http://$HEAD_IP:8000" \
      --output-dir /checkpoints \
      --save-steps "$SAVE_STEPS" \
      --num-generations "$NUM_GENERATIONS" \
      --learning-rate "$LEARNING_RATE" \
      --timeout "$TIMEOUT"
  fi
